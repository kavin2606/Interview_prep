ord() -> get the ascii value of a character
chr() -> convert the ascii value to character vale 
len() -> gets the size of the string
reversed() -> to loop backwards in for loop
string[i:j] -> returns string from i till j-1 and not j
a={} -> initializing the dictionary
a[ch]=1 -> add/update elements to a dictionary
if(ch in a) -> returns true if element is in dictionary and viceversa
string[3:],string[3:]-> returns the first three and last three values of a string
not -> negation of a boolean(not this "!")
